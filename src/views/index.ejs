<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PokeDex</title>
    <!-- Link to the external CSS stylesheet -->
    <link rel="stylesheet" href="/estilo/principal.css">
    <style>
        .pokemon-list {
            list-style: none;
            padding: 0;
            display: flex;
            flex-wrap: wrap;
            gap: 10px; /* Espaçamento entre os quadrados */
        }
        .pokemon-item {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 100px; /* Largura dos quadrados */
            height: 100px; /* Altura dos quadrados */
            background-color: #f0f0f0; /* Cor de fundo dos quadrados */
            border: 1px solid #ccc; /* Borda dos quadrados */
            border-radius: 8px; /* Cantos arredondados dos quadrados */
            text-align: center;
            font-size: 14px;
            text-decoration: none;
            color: #333;
        }
        .pokemon-item a {
            display: block;
            width: 100%;
            height: 100%;
            line-height: 100px; /* Alinha o texto verticalmente */
            color: #333;
            text-decoration: none;
        }
        .pokemon-item a:hover {
            background-color: #e0e0e0; /* Cor de fundo ao passar o mouse */
            color: #007bff; /* Cor do texto ao passar o mouse */
        }
    </style>
</head>
<body>
    <h1>PokeDex</h1>
    <!-- Loop through the results and create a link for each Pokémon -->
    <ul class="pokemon-list">
        <% results.forEach(pokemon => { %>
            <li class="pokemon-item">
                <a href="/<%= pokemon.name %>"><%= pokemon.name %></a>
            </li>
        <% }); %>
    </ul>
</body>
</html>
